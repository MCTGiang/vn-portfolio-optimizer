# Daily Progress Log — Thursday 09/04/2026
**Phase:** Week 2 — Data Validation & Returns Calculation
**Time invested:** ~1h (evening, GMT+7)
**Cumulative progress:** ~38% of Project 1

## Tasks completed today
- [x] Refine and finalize `src/features.py` — renamed functions (calc_annualized_return → annualized_return), added `TRADING_DAYS` constant, `price_col` parameter, `method` param in build_returns_matrix(), `__main__` entry point, `all_ticker_stats()` — `src/features.py`
- [x] Commit features.py to GitHub — `feat: implement features.py — returns, annualized stats, returns matrix for 30 VN30 tickers`

## Tasks not completed (reason)
- [ ] Plot return distribution per ticker — *Reason: ran out of time; moved to Fri 10/04*
- [ ] Spot-check annualized return for VCB vs Excel — *Reason: same; moved to Fri 10/04*

## Code changes
| File | Change type | Description |
|------|-------------|-------------|
| src/features.py | modified | Renamed annualized functions, added TRADING_DAYS constant, price_col param, method param, __main__ block, all_ticker_stats() |

## Git commits
- `feat: implement features.py — returns, annualized stats, returns matrix for 30 VN30 tickers`

## Key decisions
- `calc_returns()` and `calc_log_returns()` do NOT drop NaN — callers handle `.dropna()` for flexibility
- `build_returns_matrix()` uses inner join (dropna) so all tickers share the same date index
- `method='simple'` default in build_returns_matrix() — enforces MPT convention at the call site

## Issues encountered
| Issue | Resolution |
|-------|------------|
| None | — |

## Tomorrow — Top 3 priorities
1. 🔴 Spot-check: manually verify VCB annualized return for 2024 vs Excel (~20m)
2. 🔴 Plot return distribution histogram per ticker — identify most volatile stock (~30m)
3. 🟡 Read MPT basics: Expected Return, Variance, Covariance intuition (~30m)

## Week 2 milestone progress
- [x] ETL output validated: 30 tickers, row counts, nulls, price check ✅
- [x] src/update_db.py CLI complete ✅
- [x] src/features.py written, refined, committed ✅
- [ ] Returns spot-checked vs Excel
- [ ] Return distribution plotted
**On track:** Yes
