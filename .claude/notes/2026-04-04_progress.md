# Daily Progress Log — Saturday 04/04/2026
**Phase:** Week 2 — Data Validation & ETL Hardening
**Time invested:** ~3h (GMT+7)
**Cumulative progress:** ~28% of Project 1

## Tasks completed today
- [x] Restart Jupyter kernel (frozen from previous long-running update_db session)
- [x] Validate all 30 tickers via `load_from_db()`: row counts, date ranges, null check
- [x] Confirm OCB 1,289 rows (listed later than others — expected) and SHB 1,304 rows (3 missing days — normal)
- [x] Verify VCB closing price on 03/04/2026: 57,700 VND — matches CafeF ✅
- [x] Write `src/update_db.py` as standalone CLI with argparse (`--tickers`, `--start`, `--replace`)
- [x] Plot VCB and VNM price history 2021–2026 → saved to `reports/vcb_vnm_price_history.png`
- [x] Commit and push all work

## Tasks not completed (reason)
- [ ] Read Simple Return vs Log Return theory — *Reason: saved for Sun/Mon; validation took 3h*

## Code changes
| File | Change type | Description |
|------|-------------|-------------|
| src/update_db.py | created | CLI wrapper: argparse `--tickers`, `--start`, `--replace`; calls `update_db()` |
| reports/vcb_vnm_price_history.png | created | Line chart of VCB and VNM close prices 2021–2026 |

## Git commits
- `feat: ETL pipeline, 30 VN30 stocks in SQLite, VCI source, update_db script`

## Key decisions
- `update_db.py` uses `argparse` not a config file — simpler for a CLI tool with 3 flags
- `--replace` flag deletes existing rows before insert — useful for fixing bad data without manual SQL
- Notebook path for DB: `../data/raw/portfolio.db` (working dir is notebooks/) — documented in notebook header

## Issues encountered
| Issue | Resolution |
|-------|------------|
| Jupyter cells stuck at `[*]` with no output — kernel frozen from previous run | Kernel → Restart; cells re-ran cleanly |
| Notebook path issue: `data_loader.py` uses `__file__`-relative path, but notebook CWD is `notebooks/` | Load via `load_from_db()` function (which resolves path correctly) instead of raw sqlite3 |

## Tomorrow — Top 3 priorities
1. 🔴 READ: Simple Return vs Log Return (30 min) — theory needed before coding
2. 🔴 Set up Claude Code + CLAUDE.md for AI-assisted development workflow
3. 🟡 Start features.py: `calc_returns()` and `calc_log_returns()`

## Week 2 milestone progress
- [x] Validate ETL output: row counts, null check, date range confirmed
- [x] VCB price verified against market data ✅
- [x] VCB + VNM price chart saved to reports/
- [x] src/update_db.py CLI complete
- [ ] src/features.py: calc_returns(), calc_log_returns(), returns_matrix
- [ ] returns_matrix verified against Excel
**On track:** Ahead — ETL + validation done by Day 3 (was Week 2 scope)
