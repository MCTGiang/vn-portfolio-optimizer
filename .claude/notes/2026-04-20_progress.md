---
# Daily Progress Log — Monday 20/04/2026
**Phase:** Week 4 buffer + Week 5 early — Optimizer + Streamlit Dashboard
**Time invested:** ~7h (extended session — well beyond normal 1–1.5h weekday)
**Cumulative progress:** ~70% of Project 1

## Tasks completed today

### Week 4 — src/optimizer.py (fully complete)
- [x] Learned MVP theory — w^T×Σ×w formula, diversification benefit visualization
- [x] Learned `scipy.optimize.minimize`: constraints dict, bounds list, SLSQP solver, `OptimizeResult` structure
- [x] Implemented `portfolio_variance(weights, cov_mat)` — objective function returning `float`
- [x] Implemented `min_variance_portfolio(tickers, start, end)` — full optimizer pipeline
- [x] Fixed end date: auto-reads `summary['end_date'].max()` from DB instead of hardcoded string
- [x] Added input validation: missing tickers raise `ValueError` before any computation
- [x] Added short-data warning: tickers with < 252 rows print `⚠️` warning (non-fatal)
- [x] Implemented `display_portfolio(result)` — formatted CLI table with allocation + metrics
- [x] Added CLI entry point (`__main__` with argparse `--tickers`, `--start`, `--end`)
- [x] Tested 5 ticker combos — 30 VN30 tickers yields Vol reduction 33.9% vs equal weights
- [x] Tested edge cases: 2 high-corr tickers, high-vol (PDR/HVN), invalid ticker → `ValueError`
- [x] Committed: `feat: minimum variance portfolio optimizer, tested on 5 ticker combos`

### Week 5 — app/app.py Streamlit dashboard (major milestone)
- [x] Built complete `app/app.py` — production-ready Streamlit dashboard
- [x] Sidebar: ticker multiselect, DB summary (date range + row count), model info caption
- [x] Portfolio metrics row: 4 `st.metric` cards — Return, Volatility, Sharpe, Active Positions
- [x] Donut chart (Plotly) — groups < 3% into "Khác/Others", legend right-aligned
- [x] Bar chart — MVP vs Equal Weights grouped comparison for all 3 metrics
- [x] Correlation heatmap (`px.imshow`) — auto-scaling font size by ticker count
- [x] Allocation detail table — sortable, active positions highlighted in blue
- [x] Language toggle — VI/EN full bilingual via `LANG` dict + `st.session_state`
- [x] UI/UX round 1: fixed donut labels, scaled heatmap font, warm bar chart colors

## Tasks not completed (reason)
- [ ] Deploy to Streamlit Cloud — *Reason: app.py not committed yet; deploy requires pushed code*
- [ ] Test public URL from mobile — *Reason: depends on deployment*
- [ ] User acceptance test (friend) — *Reason: depends on deployment*

## Code changes
| File | Change type | Description |
|------|-------------|-------------|
| src/optimizer.py | created | `portfolio_variance()`, `min_variance_portfolio()`, `display_portfolio()`, CLI; ~239 lines |
| app/app.py | created | Full Streamlit dashboard: metrics, donut, bar chart, heatmap, alloc table, VI/EN toggle |

## Git commits
- `feat: minimum variance portfolio optimizer, tested on 5 ticker combos`
- *(app/app.py uncommitted — to commit tomorrow)*

## Key decisions
- `portfolio_variance()` returns `float()` explicitly — scipy requires scalar, not numpy scalar
- Validation runs before `expected_returns()` call — prevents NaN warnings from leaking to user
- Weights < 1e-6 zeroed out and re-normalized after optimization (numerical noise cleanup)
- `RISK_FREE_RATE = 0.045` (SBV Vietnam reference rate) baked into optimizer; parameterizable in P2
- Dashboard uses `@st.cache_data(ttl=3600)` on optimizer call — avoids re-running on every interaction
- Donut groups tickers < 3% weight into "Others" to keep chart readable on large portfolios

## Issues encountered
| Issue | Resolution |
|-------|------------|
| DB validation ran after `expected_returns()` → NaN warnings appeared before `ValueError` | Moved `missing` check above all computation calls |
| `SyntaxError`: orphan `else` block after `with st.sidebar` | Removed 2 errant lines from sidebar block |

## Tomorrow — Top 3 priorities
1. 🔴 `git add app/app.py && git commit` — push dashboard to GitHub
2. 🔴 Deploy to Streamlit Cloud: connect GitHub repo, set entry point `app/app.py`
3. 🟡 Verify public URL works end-to-end; test on mobile

## Week 4 milestone progress
- [x] `scipy.optimize.minimize` theory: constraints, bounds, SLSQP ✅
- [x] `portfolio_variance()` objective function ✅
- [x] `min_variance_portfolio()` returns weights, return, volatility, Sharpe ✅
- [x] `sum(weights)==1`, all `weights >= 0` verified ✅
- [x] Volatility < 20.67% baseline confirmed ✅ (33.9% reduction on 30 tickers)
- [x] Tested on 5+ ticker combinations ✅
- [x] `src/optimizer.py` committed ✅
**On track:** Ahead — Week 4 complete + majority of Week 5 done in one session
