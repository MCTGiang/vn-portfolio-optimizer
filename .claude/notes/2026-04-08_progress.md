# Daily Progress Log — Wednesday 08/04/2026
**Phase:** Week 2 — Data Validation & Returns Calculation
**Time invested:** ~2.5h (extended evening, GMT+7)
**Cumulative progress:** ~38% of Project 1

## Tasks completed today
- [x] Reviewed SQLite concepts: schema design, INSERT OR IGNORE, SELECT with GROUP BY, Python sqlite3 connection
- [x] Studied `src/data_loader.py` line by line: DB_PATH, get_connection(), fetch_ticker_vci(), insert_to_db(), get_all_tickers(), get_db_summary(), update_db(), load_from_db()
- [x] Studied `src/update_db.py` line by line: argparse setup, sys.path injection, entry point pattern
- [x] Reviewed OHLCV data structure and vnstock3 VCIQuote vs yfinance comparison
- [x] Read MPT theory: Simple Return vs Log Return — when to use each; annualization ×252 and ×√252
- [x] Data quality checks: dtypes ✅, 0 nulls across all 30 tickers ✅, 67 missing dates = VN holidays ✅, 0 price jumps >10% = no adjustment needed ✅
- [x] Implemented and tested `src/features.py`: calc_returns(), calc_log_returns(), annualized_return(), annualized_volatility(), ticker_stats(), build_returns_matrix(), all_ticker_stats() — `src/features.py`
- [x] Generated full stats table for all 30 VN30 tickers — LPB 47.25% top, SAB -7.37% bottom
- [x] Committed and pushed to GitHub

## Tasks not completed (reason)
- [ ] Spot-check VCB annualized return vs Excel — *Reason: ran out of time; scheduled Fri 10/04*
- [ ] Plot return distribution histograms — *Reason: same; scheduled Fri 10/04*

## Code changes
| File | Change type | Description |
|------|-------------|-------------|
| src/features.py | created | 7 functions + TRADING_DAYS=252 constant + `__main__` entry point; 196 lines |

## Git commits
- `feat: implement features.py — returns, annualized stats, returns matrix for 30 VN30 tickers`

## Key decisions
- Simple return for MPT (Covariance Matrix, portfolio_stats); log return for statistical analysis only
- `TRADING_DAYS = 252` as module-level constant — single source of truth for annualization
- `calc_returns()` / `calc_log_returns()` do NOT drop NaN — callers handle `.dropna()` for flexibility
- `build_returns_matrix()` uses inner join (dropna) so all tickers share the same aligned date index
- `method='simple'` default in `build_returns_matrix()` — enforces MPT convention at the call site

## Issues encountered
| Issue | Resolution |
|-------|------------|
| None | — |

## Next session (Fri 10/04) — Top 3 priorities
1. 🔴 Spot-check: verify VCB annualized return for 2024 vs Excel — 1 value, manual formula (~20m)
2. 🔴 Plot return distribution histogram per ticker — identify most volatile stock (~30m)
3. 🟢 Project is back on track — both tasks fit in the Fri 1h slot

## Week 2 milestone progress
- [x] ETL validated: 30 tickers, row counts, nulls, VCB price check ✅
- [x] src/update_db.py CLI complete ✅
- [x] MPT theory: simple vs log return, annualization ×252 / ×√252 ✅
- [x] src/features.py: all 7 functions, tested, committed ✅
- [ ] Returns spot-checked vs Excel — Fri 10/04
- [ ] Return distribution histogram plotted — Fri 10/04
**On track:** Yes — 2 missed days (06–07/04) absorbed; carry-overs fit Fri 10/04 slot
